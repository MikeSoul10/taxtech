# Variables PÚBLICAS del frontend. Todo lo prefijado con VITE_ queda incrustado
# en el bundle del navegador: NUNCA pongas secretos aquí (e.firma, contraseñas,
# API keys). Los secretos viven solo en el backend (ver SECURITY.md).
VITE_API_BASE_URL=http://localhost:4000/api